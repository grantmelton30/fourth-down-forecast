__pycache__/
.pytest_cache/
# data/prop_quotes.jsonl and data/bdl/ are deliberately NOT ignored. They are archives, not
# caches: the balldontlie pull came from a paid key that has been cancelled and cannot be
# repeated, and no free historical prop source exists at any price. An uncommitted quote is
# one that can never be part of a backtest.
